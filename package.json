{
  "name": "pigpg",
  "version": "1.0.0",
  "description": "PigPG is a programming space for flexible employees.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "doc": "turbo doc",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo build --filter=docs^... && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@changesets/cli": "^2.27.9",
    "turbo": "^2.2.3"
  },
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=22"
  }
}
